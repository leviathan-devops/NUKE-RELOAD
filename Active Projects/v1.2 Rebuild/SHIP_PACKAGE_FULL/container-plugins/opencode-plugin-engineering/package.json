{
  "name": "opencode-plugin-engineering",
  "version": "1.0.0",
  "description": "OpenCode plugin engineering knowledge - exposes plugin patterns, hooks, and SOPs as a tool",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --bundle"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.3.6"
  },
  "dependencies": {
    "zod": "4.1.8"
  }
}
